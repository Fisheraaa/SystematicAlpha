# 📄 项目需求文档 (PRD): 系统化阿尔法研究与交易框架

**项目名称：** Systematic Alpha Research & Trading Framework (Bias-Controlled, Regime-Aware)  
**文档版本：** 2026-V1.0  
**核心理念：** 构建一个**可复现、无偏、可扩展**的工业级量化研究最小闭环，强调底层工程的鲁棒性与金融逻辑的严谨性[cite: 1]。

---

## 1. 项目目标
构建从“假设驱动”到“组合构建”的全流程量化平台，核心考核指标为回测的**真实性**与**逻辑的可解释性**[cite: 1]。
*   **完整闭环**：假设 $\rightarrow$ 因子 $\rightarrow$ 验证 $\rightarrow$ 策略 $\rightarrow$ 回测 $\rightarrow$ 风控 $\rightarrow$ 组合。
*   **偏差控制**：严禁未来函数 (Look-ahead bias)、数据拆分泄漏 (Data leakage) 及理想化交易。
*   **实战验证**：包含真实交易成本，必须通过样本外 (Out-of-sample) 测试。

---

## 2. 技术与工程约束

### 2.1 技术栈要求[cite: 1]
*   **语言**：Python 3.x (核心逻辑)；Numba/Polars (性能优化加速)。
*   **工具**：Pandas/NumPy (矩阵运算)；Matplotlib/Plotly (可视化报告)。
*   **存储**：本地化 Parquet/CSV 或轻量级 SQLite[cite: 1]。

### 2.2 行为禁止项 (直接判失败)
*   ❌ **禁止**随机打乱 (Random Split) 训练集与测试集（必须按时间线性切分）。
*   ❌ **禁止**在因子计算中使用 $t$ 时刻以后的任何信息。
*   ❌ **禁止**黑盒 AutoML 或未经逻辑验证的深度学习模型。
*   ❌ **禁止**忽视交易成本（手续费、印花税、滑点）[cite: 1]。

---

## 3. 数据与因子系统 (Data & Factor Layer)

### 3.1 数据处理规范
*   **基础行情**：日频 OHLCV (Open, High, Low, Close, Volume) 数据，范围 $\ge 5$ 年[cite: 1]。
*   **预处理**：实现缺失值填充 (Ffill) 与异常值去极值 (Winsorize/Clip)。
*   **一致性约束**：特征 $f_t$ 仅可见 $t$ 时刻及之前数据，标签 $label$ 必须为 $return_{t+1}$。

### 3.2 必须实现的因子类别

*   **均值回归 (Mean Reversion)**：Z-score, RSI。
*   **趋势/动量 (Momentum)**：5日/20日收益率。
*   **波动率/环境 (Regime)**：Rolling Std (滚动标准差), ATR (真实波幅平均值)。
*   **计算约束**：所有滚动窗口必须设置 `closed='left'` 或进行 `.shift()` 处理，确保无未来数据。

---

## 4. 因子验证系统 (Validation Layer)

在进入回测前，必须对因子进行严格的统计检验：

*   **IC 分析 (Information Coefficient)**：
    $$IC = corr(factor_t, return_{t+1})$$
    输出平均 IC、IC 标准差及 $IC\_IR = \frac{mean(IC)}{std(IC)}$。
*   **IC Decay (衰减分析)**：计算 $k=1, 2, 3, 5$ 日的 IC 衰减情况。
*   **分层回测 (Quantile Analysis)**：
    1.  每日按因子值对标的进行 5 组 (Quintiles) 分类。
    2.  计算 Q5 (多头组) vs Q1 (空头组) 的 Spread (收益差)。

---

## 5. 策略与回测引擎 (Strategy & Backtest Engine)

### 5.1 策略逻辑
1.  **Mean Reversion**：$|Z\text{-}score| > threshold$ 触发逆向交易。
2.  **Momentum**：$Return_{20d} > threshold$ 触发顺势交易。
3.  **Regime-Aware (核心)**：
    *   若 $Volatility > threshold$：执行均值回归。
    *   若 $Volatility \le threshold$：执行动量策略。

### 2.2 仿真回测约束[cite: 1]
*   **执行逻辑**：$t$ 时刻产生信号，$t+1$ 时刻以 Open 或 Close 价格撮合。
*   **成本模型**：
    $$PnL = \sum (Return - Fee - Slippage)$$
    其中 Fee 设置为 **0.05%**，Slippage 设置为 **0.01%–0.1%**。
*   **冗余性**：必须记录每日 Cash (现金) 与 Equity (权益) 变动。

---

## 6. 风控与组合管理 (Risk & Portfolio Layer)

*   **头寸管理 (Position Sizing)**：实现风险溢价对齐，即 $Position \propto \frac{1}{Volatility}$。
*   **回撤拦截**：当 $Drawdown > threshold$ 时，强制降低仓位或平仓。
*   **组合优化**：
    *   Baseline：等权重 (Equal Weight)。
    *   进阶：风险平价 (Risk Parity)，权重计算公式为 $$w_i = \frac{1/\sigma_i}{\sum (1/\sigma_j)}$$。

---

## 7. 验收标准与产出要求

### 7.1 核心指标 (Metrics)
*   **Sharpe Ratio** (夏普比率)、**Max Drawdown** (最大回撤)、**Annual Return** (年化收益)。
*   **Turnover** (换手率) 分析。

### 7.2 报告必须包含的内容
1.  **Failure Cases (失效分析)**：明确描述策略在何种行情下会失效及原因。
2.  **结果对比**：展示 Train (训练集) 与 Test (测试集) 的性能差异，评估过拟合程度。
3.  **可视化图表**：净值曲线 (Equity Curve)、回撤曲线、IC 稳定性直方图[cite: 1]。

### 7.3 项目完成标志
*   系统能回答：**“如果交易成本翻倍，该策略是否依然具备统计学显著性？”**
*   系统能回答：**“因子在测试集中的 IC 是否保持同向稳定性？”**[cite: 1]

---

**结尾建议：**
该 PRD 要求的是构建一个**研究平台**而非单一脚本。在实现时，请务必关注代码的可读性与函数接口的通用性，以便后期接入更多因子类型[cite: 1]。

对于后续执行，您希望先从“数据抓取与本地持久化”模块开始，还是直接定义“因子类接口”？