# PRD 中文导读 — 系统化阿尔法研究框架
**v1.1 — 已锁定决策版本**

> 本文档是英文 PRD v1.1 的配套说明。所有已确认的设计决策在本版本中已
> 全部对齐，不需要再做方向性讨论。代码、注释、commit 信息全部用英文；
> 这份文档是你自己看的。

---

## 已锁定的核心决策（不再讨论）

| 决策项 | 结论 |
|---|---|
| 资产类别 | A 股，CSI 300 + CSI 500 |
| 多空方向 | 纯多头（Long-only） |
| 数据源 | Tushare Pro（学生认证后积分可用） |
| 回测结构 | 三条独立回测线：`csi300` / `csi500` / `combined` |
| `combined` 分配 | 40% CSI 300 子组合 + 60% CSI 500 子组合 |
| 基准 | `csi300` → CSI 300 TR；`csi500` → CSI 500 TR；`combined` → 40/60 混合 |
| 再平衡频率 | 月度，每月第一个交易日 |
| 持仓数量 | 每次 20–40 只（低波动 40 只，高波动 20 只） |

---

## 一、整体逻辑：为什么这样设计

这个 project 的定位是"能让量化面试官点头的研究报告"，而不是"跑通了就行的课程作业"。区别在三个地方：

**第一，偏差控制是工程约束，不是事后检查。**
数据层、因子层、回测层各有独立的防护机制，任何一层出问题，下游结果全部无效。这种设计让你在面试时可以说："我的回测结果是在三重偏差防护下跑出来的，不是拍脑袋的数字。"

**第二，统计显著性，不是结果好看。**
因子必须通过 IC t-test（p < 0.05），策略必须通过 Walk-Forward 多窗口验证。单次样本外 Sharpe 高不算数，你要展示的是"这个策略在 8 个独立时间窗口里有 X% 的胜率"。

**第三，三-run 对比设计让报告有研究深度。**
同一套因子体系，分别在大盘（CSI 300）、中盘（CSI 500）和混合组合三个场景下独立验证，然后对比结果。这比"跑了一条回测"有更强的说服力，因为你可以用数据回答：CSI 500 的超额收益能否覆盖更高的交易成本？哪个 universe 里动量因子更有效？mixed 组合是否真的比单一 universe 更优？

---

## 二、三-run 设计的具体含义

这是 v1.1 最重要的结构性决定，需要理解清楚，否则实现时容易混乱。

**三条回测线的逻辑关系：**

```
因子计算（全量股票池，一次性完成）
        │
        ├─── CSI 300 PIT universe ──► csi300 run
        │         ↓ 对比 CSI 300 TR 基准
        │
        ├─── CSI 500 PIT universe ──► csi500 run
        │         ↓ 对比 CSI 500 TR 基准
        │
        └─── 两者的并集 ──────────► combined run
                  ↓ 对比 40% CSI300 TR + 60% CSI500 TR 混合基准
```

**关键：因子只计算一次。** 全部股票的 Mom5d、ZScore20d 等因子值存在同一批 Parquet 里，三个 run 在各自的再平衡日去切片属于自己 universe 的那些股票。这样三个 run 用的是完全相同的因子数值，性能差异 100% 来自 universe 构成和成本模型的不同，不是因子计算的差异。

**combined run 的组合逻辑：**
不是把 700 只股票扔进一个大池子选 40 只。而是：
- 在 CSI 300 的 universe 里独立跑信号，选出 Top N 只，构成 CSI 300 子组合
- 在 CSI 500 的 universe 里独立跑信号，选出 Top N 只，构成 CSI 500 子组合
- 两个子组合按 40/60 拼在一起，总仓位 95%（5% 现金缓冲）

这样做的好处：attribution 清晰。你可以在报告里说"combined 的超额收益里，43% 来自 CSI 300 子组合，57% 来自 CSI 500 子组合"，比黑箱混选更有说服力。

**IC 权重分开校准：**
因子验证（IC 分析）在 CSI 300 和 CSI 500 两个 universe 里独立跑，得到两套 IC 权重向量。combined run 用这两套权重的 40/60 加权平均，不单独重新校准。这保持了可解释性。

---

## 二、数据层：为什么选 Tushare Pro

| 对比维度 | Tushare Pro | AKShare | yfinance |
|---|---|---|---|
| 历史成分股快照 | ✅ 有（这是核心） | ❌ 无 | ❌ 无 |
| A 股日频行情 | ✅ 完整 | ✅ 完整 | 部分 |
| 复权因子 | ✅ 独立接口 | 内置（精度稍低） | 内置 |
| 免费额度 | 每分钟限速，够用 | 完全免费 | 完全免费 |
| 注册难度 | 邮件注册即可 | 无需注册 | 无需注册 |

**选 Tushare 的唯一原因**：`index_weight` 接口提供历史每个时间点的成分股列表。
没有这个数据，你的回测就有幸存者偏差，所有结果都不可信。

AKShare 作为备用：如果 Tushare 限速，用 AKShare 拉行情数据，
但成分股快照要从你已经存好的 Tushare 数据里读，不能从 AKShare 重新拉。

---

## 三、Universe 模块：这是你项目的核心竞争力

**幸存者偏差是什么？**

今天的沪深 300 里有 300 只股票，它们是"活下来"的。
2018 年的某些股票现在已经退市、或者被剔出指数了。
如果你用今天的 300 只股票回测 2018 年，你在 2018 年
就"知道"了这些公司能撑到 2024 年，相当于作弊。

**怎么解决？**

Tushare 的 `index_weight` 表里，每一行是"某日期、某股票在某指数里的权重"。
你在构建 2018 年 3 月的股票池时，只用 2018 年 3 月 1 日这条历史记录，
不看 2024 年的数据。这叫 Point-in-Time（点位）成分股。

**代码逻辑的核心**：

```python
# 错误做法（幸存者偏差）
today_constituents = tushare.get_current_constituents("000300.SH")
backtest(data=historical_data, universe=today_constituents)  # 作弊！

# 正确做法（点位数据）
def get_universe(date):
    history = load("data/raw/index_weight/000300.SH.parquet")
    snapshot = history[history["date"] <= date].groupby("ts_code").last()
    return snapshot.index.tolist()  # 这才是 date 那天实际在指数里的股票
```

**面试时怎么说**：
> "我在 universe 构建上做了 point-in-time 处理，每个历史时间点的股票池
> 严格等于那个时间点的指数实际成分股，而不是用今天的成分股回看历史。
> 这消除了幸存者偏差，否则回测结果会虚高 0.3–0.8 个 Sharpe 左右。"

---

## 四、因子库：6 个因子的选择逻辑

这 6 个因子不是随机选的，它们覆盖了三个维度，形成一个完整的"市场行为地图"：

| 因子 | 类型 | 捕捉的现象 | A 股特殊处理 |
|---|---|---|---|
| Mom5d | 短期动量 | 近期涨势延续 | skip 最近 1 天（A 股短期反转强） |
| Mom20d | 中期动量 | 1 个月趋势延续 | skip 最近 2 天 |
| ZScore20d | 均值回归 | 价格偏离均值后回归 | Winsorise ±3σ |
| RSI14d | 均值回归 | 超买超卖极值 | 取负值作为预测变量 |
| RVol20d | 波动率 | 风险水平（用于仓位） | 不直接用于选股 |
| ATR14d | 波动率 | 日内振幅（用于仓位） | 比 RVol 更鲁棒于涨跌停 |

**A 股特殊处理的原因**：

Mom5d 为什么 skip 1 天？
A 股短期（1-5 天）存在显著的反转效应，这和美股相反。
如果你包含最近 1 天的收益，5 日动量因子实际上会部分捕捉反转，
IC 会偏低甚至变负。跳过最后 1 天后，你捕捉的是更纯粹的动量部分。

RVol 和 ATR 为什么两个都要？
涨跌停时，收盘价会被锁死在±10%，这会让 RVol（基于收益计算）
低估真实的价格波动。ATR 用日内高低价差计算，涨跌停日仍然能反映
实际的价格压力。两者结合更全面。

---

## 五、因子验证：IC t-test 是你的核心武器

**IC 是什么**：

IC = Spearman 秩相关（因子值 at t，次日收益 at t+1）

IC > 0 意味着因子值高的股票，次日表现更好。
IC < 0 意味着反向预测（取负值后用）。
|IC| 的大小衡量预测强度，通常 |mean_IC| > 0.02 算有意义。

**为什么用 Spearman 不用 Pearson**：

A 股有涨跌停，极端收益（±10%）不是真实的收益分布，
被强制截断了。Pearson 对极值敏感，Spearman 只看排名，
对这种截断更鲁棒。

**t-test 的意义**：

```
t = mean(IC) / (std(IC) / sqrt(T))
```

这个检验告诉你：因子的平均 IC 是不是真的大于 0，
还是只是随机波动让它看起来大于 0。

p < 0.05 意味着：假设因子真的没有预测力，
观察到这么高的 mean_IC 的概率不足 5%。
换句话说，你有 95% 的信心说因子有效。

**面试里你可以说**：
> "我的 Mom20d 因子在 IS 期间的 ICIR 是 0.38，IC 均值的 t-stat 是 4.2，
> p-value < 0.001，统计上显著。在 OOS 期间 IC 方向一致性维持在 58%，
> 说明因子没有严重衰减。"

这种表达方式和"我的因子 IC 是 0.05"完全是两个层次。

---

## 六、Regime Detection：最重要的创新点

**核心逻辑**：

市场不是一个稳态系统。动量策略在慢牛里有效，
均值回归策略在震荡市里有效。用一套固定的因子权重
做所有行情，是在假设市场永远处于同一状态。

**你的解法**：

用沪深 300 指数的 20 日波动率的历史百分位（过去 252 天）
来判断当前行情状态：

- 百分位 ≤ 50%：低波动，偏动量（Mom 权重 0.6，Mom20 权重 0.4）
- 50%–70%：过渡区，线性插值
- 百分位 > 70%：高波动，偏均值回归（ZScore 0.5，RSI 0.5）

**额外加一层（ADX）**：

高波动里有两种情况：
1. 有方向的高波动（比如单边下跌）→ 动量仍然有效
2. 无方向的高波动（来回震荡）→ 才真正需要均值回归

ADX > 25 说明趋势强，即使在高波动环境里也保留 30% 动量权重。
ADX ≤ 25 说明无方向震荡，全用均值回归。

**这个设计的价值**：

- 面试 talking point：从"我做了个回测"变成"我做了一个自适应系统"
- 研究价值：可以单独分析不同 Regime 下的策略表现差异，产生更丰富的分析
- 实际改进：经验上，Regime-aware 策略比静态权重策略的 Drawdown 通常
  小 3-8 个百分点，但 Sharpe 不一定更高（Regime 识别不完美会带来额外成本）

---

## 七、回测引擎：A 股成本必须精确

**为什么成本这么重要**：

如果你每月换仓 30%，单次成本 0.25%（买+卖），
一年 12 次 × 0.25% × 2（来回）= 6% 的成本！
一个年化 10% 的策略，成本就吃掉 60%。

**A 股成本结构**（2024 年数据，印花税 2023 年 8 月已减半）：

| 项目 | 方向 | 费率 |
|---|---|---|
| 券商佣金 | 双向 | 0.025%（最低 5 元） |
| 印花税 | 仅卖出 | 0.05% |
| 过户费（沪市）| 双向 | 0.001% |
| 滑点（沪深 300）| 双向 | 0.05% |
| 滑点（中证 500）| 双向 | 0.10% |

买入总成本：约 0.076%
卖出总成本：约 0.176%
完整换仓（买+卖）：约 0.25%（CSI 300），0.30%（CSI 500）

**T+1 规则**：

买入的股票次日才能卖出。你的回测引擎必须记录每笔持仓的买入日期，
卖出时检查是否满足 T+1 要求。这个约束会影响极端行情下的逃跑能力，
是 Failure Case 分析里要重点讨论的内容。

---

## 八、Walk-Forward：让你的 OOS 有说服力

**为什么 Walk-Forward 比普通 train/test split 好**：

普通做法：前 70% 训练，后 30% 测试，只有 1 个 OOS 观测值。
你不知道那个好（或坏）的 Sharpe 是真实能力还是运气。

Walk-Forward：滑动时间窗口，产生 8-13 个独立的 OOS 窗口。
你可以说：
> "策略在 13 个 OOS 窗口中，有 9 个 Sharpe > 0.5，
>  胜率 69%，平均 OOS Sharpe 0.82，标准差 0.31。"

这个表述和单次 "OOS Sharpe 0.82" 的可信度不在同一量级。

**具体参数**：

```
训练窗口：24 个月（滚动，不扩张）
测试窗口：6 个月
步长：3 个月（测试窗口重叠 3 个月）
OOS 时间：2021-01 到 2024-12
```

**每次滑动都重新校准**：

每个窗口用训练数据重新计算因子 IC 权重和 Regime 阈值，
再在测试窗口里跑回测。这保证了每次 OOS 的参数都是
当时可以合法获取的信息，而不是未来的知识。

---

## 九、输出报告：面试前最重要的一步

**HTML 报告是核心 deliverable**。一个 `report.html` 文件，
在浏览器里打开，涵盖所有分析图表。发给面试官的 GitHub 链接
和简历里的 project description，都应该让他们能一键看到这个报告。

**5 个必须有的图表**：

1. 净值曲线 vs 沪深 300 基准（含回撤面积图 + Regime 背景色块）
2. 每个因子的 IC 时间序列（60 日滚动均值 + ±1σ 带）
3. 分层回测柱状图（Q1–Q5 的年化收益，带误差棒）
4. Walk-Forward OOS 性能分布（箱型图）
5. 换手率 & 成本拖累（月度换手率 + 累计成本）

**Failure Case 写作建议**：

不要写"策略在 2022 年有较大回撤"这种废话。
要写：
> "2022 年 3-4 月，受市场恐慌情绪影响，
>  沪深 300 在 40 个交易日内下跌 22%。
>  Regime 信号在下跌开始后 15 个交易日才切换至高波动状态，
>  这 15 天的延迟导致策略继续持有动量组合，额外亏损约 3.2%。
>  这是 Regime 信号固有的滞后性，无法通过参数调整完全消除。"

量化、有根据、承认局限性——这才是研究的态度。

---

## 十、代码规范：给简历加分的细节

**config.yaml 集中参数**

所有可调参数（回测区间、窗口大小、成本费率、阈值）
统一放在 config.yaml，代码里一个 magic number 都不要出现。

这是工程规范，面试官看代码时会注意。能展示"我理解可维护性"
比"我跑了个回测"要有分量得多。

**单元测试是护城河**

`tests/test_no_lookahead.py` 是最重要的测试：
它验证每个因子在 t 日的值与 t 日收盘价无相关性。
这个测试如果能通过，你就有实验证据证明你的因子没有未来数据。

面试时可以说："我写了单元测试自动检测 look-ahead bias，
不是人工 review，是跑测试套件验证的。"

**README 结构建议**：

```markdown
# Systematic Alpha Research Framework

## Key Results
| Metric | IS (2015–2020) | OOS (2021–2024) |
|---|---|---|
| Sharpe Ratio | X.XX | X.XX |
| Max Drawdown | XX% | XX% |
| Annual Return | XX% | XX% |
| vs. CSI 300   | +X.X% | +X.X% |

## Methodology
[3 paragraphs: factor research → regime detection → backtest]

## Setup
pip install -r requirements.txt
python main.py --fetch --validate --backtest --report

## Key Design Decisions
1. Point-in-time universe (no survivorship bias)
2. Walk-forward validation (13 OOS windows)
3. A-share specific cost model

## Known Limitations
[直接写失效 case，显示你的研究诚意]
```

---

## 十一、时间规划参考

| 周次 | 主要工作 | 完成标志 |
|---|---|---|
| 第 1 周 | 数据层 + Universe | Parquet 存好，手动核对 5 个历史日期的成分股 |
| 第 2 周 | 因子库 | 6 个因子计算完，lookahead 单元测试通过 |
| 第 3 周 | 因子验证 | IC 表格出来，至少 2 个因子 p < 0.05 |
| 第 4 周 | Regime + 基础回测 | 等权无 Regime 版本跑通，成本模型验证 |
| 第 5 周 | Risk Parity + Regime 策略 | 完整策略，Walk-Forward 跑完 |
| 第 6 周 | 报告 + README + 整理 | HTML 报告生成，GitHub 公开 |

---

## 十二、面试时的核心 Narrative

把这个 project 的故事压缩成 2 分钟版本：

> 我做了一个针对 A 股 CSI 300 和 CSI 500 的系统化因子研究框架，设计上有三个层次的差异化：
>
> **第一层，数据严谨性。** 我用 Tushare 的历史成分股快照构建了 point-in-time universe，消除了幸存者偏差。同时对因子计算做了自动化 look-ahead bias 检测——写了单元测试，不是人工 review。
>
> **第二层，统计严谨性。** 6 个因子全部通过 Spearman IC + t-test 做了显著性检验，ICIR 阈值 0.3、p-value 阈值 0.05，不显著的因子不进回测。然后用 Walk-Forward 在 13 个样本外窗口里验证策略一致性，给出的是性能分布，不是单次 OOS 数字。
>
> **第三层，分析深度。** 我设计了三条独立回测线——纯大盘（CSI 300）、纯中盘（CSI 500）和混合组合——分别用对应的基准评估，然后对比三者的表现差异。这让我可以用数据回答：CSI 500 更高的 alpha 能否覆盖更高的交易成本？动量因子在大盘和中盘里哪个更有效？
>
> OOS 期间（2021–2024），三个 run 的平均 Sharpe 分别是 X、Y、Z，combined 在 XX% 的时间窗口里跑赢了混合基准。

---

## 十三、版本记录

- v1.0：初始规格说明
- v1.1：锁定所有设计决策（long-only、Tushare Pro、三-run 架构、per-run 基准、图表结构、验收标准矩阵）

*这份文档仅供个人参考，项目所有对外的代码、文档、注释均用英文。*
